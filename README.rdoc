== README


YuReview setup:

* clone the Repo

* Bundle install

* rake db:create && rake db:migrate

* start Rails server (rails s)

* rake test to run test suite

* rake db:seed to seed database with some development data




